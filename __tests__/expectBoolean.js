import { expectBoolean as expBool } from '../src';
import * as test from '../test-utils';

test._nan(expBool, new TypeError('Expected boolean, got NaN'));
test._plusInfinity(expBool, new TypeError('Expected boolean, got infinity'));
test._minusInfinity(expBool, new TypeError('Expected boolean, got infinity'));
test._undefined(expBool, new TypeError('Expected boolean, got undefined'));
test._null(expBool, new TypeError('Expected boolean, got null'));
test._true(expBool, true);
test._false(expBool, false);
test._function(expBool, new TypeError('Expected boolean, got function'));
test._number(expBool, new TypeError('Expected boolean, got number'));
test._object(expBool, new TypeError('Expected boolean, got object'));
test._string(expBool, new TypeError('Expected boolean, got string'));
test._symbol(expBool, new TypeError('Expected boolean, got symbol'));
test._integer(expBool, new TypeError('Expected boolean, got number'));
test._array(expBool, new TypeError('Expected boolean, got object'));
test._typedArray(expBool, new TypeError('Expected boolean, got object'));
test._map(expBool, new TypeError('Expected boolean, got object'));
test._set(expBool, new TypeError('Expected boolean, got object'));
